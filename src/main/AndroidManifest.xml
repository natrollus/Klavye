<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.natrollus.klavye">

    <application android:allowBackup="true" android:label="@string/appisim"
        android:icon="@drawable/ikon">
        <service android:name=".Klavye" android:label="@string/appisim" android:permission="android.permission.BIND_INPUT_METHOD">
            <meta-data android:name="android.view.im" android:resource="@xml/metod"/>
            <intent-filter>
                <action android:name="android.view.InputMethod"/>
            </intent-filter>
        </service>
    </application>

</manifest>
